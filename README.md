# Sales Forecasting App

This is a simple Streamlit app for **sales forecasting** using a **multi-model ensemble** (XGBoost + CatBoost).  

## Features
- Upload CSV data and get predicted sales instantly.
- Weighted ensemble predictions based on `ensemble_config.json`.
- Download predictions as CSV.

## Setup
1. Install dependencies:
